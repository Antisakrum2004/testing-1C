{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma db push --skip-generate --accept-data-loss 2>/dev/null || true && next build",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": true
  }
}
